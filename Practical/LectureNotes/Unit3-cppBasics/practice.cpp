#include <iostream>
#include <vector>
#include <string>
#include <list>
#include <map>
#include <set>


using namespace std;

int main(int argc, char *argv[]) {

    set<int> s = {60, 30, 40, 40};

    auto t = s.find(1);

    // compute the absolute frequency of input words
    map<string, size_t> word_count;
    set<string> exclude = {"The", "but", "and", "or", "an", "a"};





}